var nome = "Daniel Fernandes"

var soma = (num1, num2) => {
    return num1 + num2
}

var sub = (num1, num2) => {
    return num1 - num2
}

var mult = (num1, num2) => {
    return num1 * num2
}

var div = (num1, num2) => {
    return num1 / num2
}

module.exports = {
    soma,
    sub,
    mult,
    div,
    nome
}